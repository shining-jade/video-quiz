# Cloud Firestore 이전 및 운영 절차

이 문서는 영상 퀴즈의 Realtime Database 데이터를 Cloud Firestore로 옮기고 배포를 검증하는 운영 기준입니다. 저장소의 애플리케이션 코드, Firebase 콘솔 설정과 기존 세트 이전은 완료됐으며, 실제 운영 배포 검증만 남아 있습니다.

## 현재 구성

- Firebase 프로젝트: `video-quiz-65798`
- Firestore 위치: `asia-southeast1`
- 인증: Firebase Authentication 익명 로그인
- 보안 규칙 원본: `firestore.rules`
- 기존 사이트: GitHub Pages의 `main` 브랜치 루트
- Firestore 이전 배포: 콘솔 설정·규칙 게시·기존 세트 이전 완료, GitHub Pages 배포 검증 예정

Firestore의 운영 데이터 구조는 다음과 같습니다.

```text
quiz_sets/{setId}
images/{setId}/q/{questionIndex}
codes/{code}
sessions/{sessionId}
sessions/{sessionId}/meta/live
sessions/{sessionId}/meta/board
sessions/{sessionId}/students/{studentId}
sessions/{sessionId}/responses/{studentId}
config/app
clock/{sampleId}
```

`quiz_sets`는 문항 배열과 진행 설정을 한 문서에 보관합니다. 이미지는 문항별 문서, 응답은 학생별 문서로 분리합니다. 학생 화면은 `meta/live`만 실시간 구독하며 다른 학생의 응답 문서는 읽지 않습니다. `clock` 문서는 서버 시각 보정을 위해 잠깐 생성한 뒤 즉시 삭제합니다.

## 1. Firestore와 인증 준비

1. Firebase 콘솔에서 프로젝트 `video-quiz-65798`을 엽니다.
2. Firestore Database가 없다면 **데이터베이스 만들기 → 프로덕션 모드**를 선택합니다.
3. 위치가 `asia-southeast1`인지 확인합니다. 생성 뒤에는 위치를 바꿀 수 없습니다.
4. Authentication → Sign-in method에서 **익명(Anonymous)** 공급자를 사용 설정합니다.
5. Firestore Database → 규칙에서 저장소의 `firestore.rules` 내용을 게시합니다.

규칙은 인증 사용자에게 앱이 사용하는 컬렉션만 허용합니다. `sessions`는 모든 재귀 하위 문서를 포함하고, 공유 퀴즈 보호를 위해 `quiz_sets` 삭제는 항상 거부합니다. 명시하지 않은 경로는 기본적으로 거부됩니다.

## 2. 기존 데이터 JSON 이전

기존 RTDB 원본은 Firebase 콘솔에서 Firestore로 직접 가져오지 않습니다. 애플리케이션의 세트 JSON 내보내기·가져오기 기능을 사용합니다.

1. 이전 배포본의 `#/sets`에서 각 세트의 **[📤 파일]**을 눌러 JSON을 내려받습니다.
2. 파일명에 세트 이름과 내보낸 날짜를 남기고, 원본과 분리된 백업 위치에 복사합니다.
3. Firestore 배포본의 `#/sets`에서 **[📥 세트 가져오기]**를 눌러 JSON을 가져옵니다.
4. 세트 제목, 다섯 문항 유형, 진행 설정과 문항 이미지가 모두 보이는지 확인합니다.
5. 가져온 세트를 다시 JSON으로 내보내 백업 파일이 정상 생성되는지 확인합니다.

기존 수업 세션과 응답은 세트 JSON에 포함되지 않습니다. 개인정보 보관 기준상 필요한 기록은 기존 시스템에서 별도로 보존하고, 신규 수업부터 Firestore에 기록합니다.

### 원본 보존 원칙

Firestore로 가져온 직후 기존 RTDB 데이터를 삭제하지 않습니다. 아래 배포 검증을 마치고, 담당자가 JSON 백업과 필요한 과거 기록의 보존 상태를 확인한 뒤에만 학교 개인정보 보관·파기 절차에 따라 원본을 정리합니다. 문제가 생기면 원본과 백업 JSON을 이용해 다시 확인할 수 있도록 검증 기간을 둡니다.

## 3. 자동 검증

배포 전에 저장소 루트에서 다음 명령을 실행합니다.

```powershell
node --test tests/*.test.js
git diff --check
```

- 모든 Node 테스트가 경고 없이 통과해야 합니다.
- 별도 정적 검색으로 생산 코드에 이전 데이터베이스의 설정 키, SDK 이름, 참조 헬퍼, 단발 읽기·트랜잭션·서버 시각 API가 남지 않았는지 확인합니다. 이 문서에서 이전 시스템을 설명하는 `RTDB`라는 명칭 자체는 역사적 문맥입니다.
- `git diff --check`는 출력 없이 종료 코드 0이어야 합니다.
- Firebase 콘솔 규칙 편집기의 구문 검사와 게시가 성공해야 합니다.

## 4. 배포

1. 코드와 문서 변경을 `main` 브랜치에 커밋하고 GitHub에 푸시합니다.
2. GitHub Pages 배포가 끝날 때까지 기다린 뒤 `https://shining-jade.github.io/video-quiz/`를 새로고침합니다.
3. 브라우저 개발자 도구에서 이전 데이터베이스 SDK가 로드되지 않고 Firestore SDK가 로드되는지 확인합니다.
4. Firebase 콘솔에서 방금 실행한 테스트 데이터가 Firestore의 예상 경로에만 생기는지 확인합니다.

## 5. 실제 배포 검증 체크리스트

교사 창과 학생 창을 따로 열고 다음을 확인합니다.

- [ ] 익명 인증 뒤 홈 화면이 오류 없이 열린다.
- [ ] 세트를 만들고 객관식·복수정답·O·X·단답형·서술형을 저장한 뒤 다시 편집할 수 있다.
- [ ] 업로드 이미지가 교사 화면과 학생 화면에 보인다.
- [ ] 같은 세트로 두 반을 시작하면 서로 다른 반 코드가 발급되고 응답이 섞이지 않는다.
- [ ] 학생 입장과 문항 열림이 실시간으로 반영된다.
- [ ] 교사와 학생 기기의 남은 시간이 1초 이내로 일치한다.
- [ ] 정답 공개 전 교사 투사 화면에 정답과 응답 분포가 노출되지 않는다.
- [ ] 자동 채점과 서술형 O/X 수동 채점이 점수에 반영된다.
- [ ] 순위, CSV 내려받기, 관리자 전체 조회가 정상이다.
- [ ] 관리자 기간 삭제가 세션 하위 문서와 반 코드만 지우고 퀴즈 세트는 보존한다.
- [ ] 세트 삭제 요청은 규칙에서 거부되고 숨기기·복원은 정상이다.
- [ ] Firestore 사용량이 예상보다 급격히 증가하지 않고 콘솔에 권한 오류가 없다.

## 6. 읽기 사용량 운영 원칙

- 학생은 `sessions/{sessionId}/meta/live` 한 문서만 실시간 구독합니다.
- 퀴즈 세트는 학생 입장 시 한 번만 읽습니다.
- 응답은 `sessions/{sessionId}/responses/{studentId}`에 문항별 필드를 병합합니다.
- 교사 대시보드만 학생과 응답 컬렉션을 구독합니다.
- 순위 요약은 문항을 닫을 때 갱신합니다.
- 관리자 전체 조회는 화면을 열거나 새로고침할 때만 실행합니다.

실시간 구독 범위를 넓히는 변경은 참가자 수만큼 문서 읽기를 늘릴 수 있으므로 배포 전에 반드시 사용량 영향을 검토합니다.

## 완료 체크리스트

- [x] Firestore SDK와 저장소 어댑터 적용
- [x] 퀴즈 세트·이미지·세션·학생·응답·관리자 기능 이전
- [x] Firestore 서버 시각 보정과 반 코드 트랜잭션 적용
- [x] `firestore.rules` 작성
- [x] 운영 문서의 Firestore 기준 갱신
- [x] Firebase 콘솔에서 규칙 게시
- [x] 기존 세트 JSON 내보내기·가져오기
- [ ] 실제 GitHub Pages 배포 검증
- [ ] 검증 기간 뒤 기존 원본 보존 또는 파기 결정 기록

## Task 8 실제 환경 검증 기록 (2026-08-14)

결과: **PARTIAL PASS — 콘솔 준비·데이터 이전과 원본 객관식 세트의 로컬 핵심 흐름 검증 완료**

- 기존 배포 앱에는 `심폐소생술` 세트 1개와 객관식 문항 `연습` 1개가 있었다. 원본 그대로 JSON을 내보내 `.migration-backup/video-quiz-rtdb-2026-08-14.json`에 로컬 보존했으며 기존 RTDB 데이터는 삭제하거나 변경하지 않았다.
- Firebase 프로젝트 `video-quiz-65798`에 프로덕션 모드 Firestore `(default)`를 `asia-southeast1` 리전에 생성하고, 저장소의 `firestore.rules`를 2026-08-14 21:24(KST)에 게시했다.
- 백업 JSON을 Firestore 세트 `MIG20260814`로 가져왔다. 재조회 결과 제목 `심폐소생술`, 문항 1개, 이미지 0개가 원본과 일치했다.
- 같은 세트로 만든 두 수업의 반 코드 `Y3WV24`, `YTMTFM`이 서로 달랐다. 두 번째 수업에서 학생 문항 반영은 측정상 약 1.02초였고, 객관식 정답 제출 뒤 교사 화면에 `1 / 1 제출`, 학생 화면에 `1 / 1 정답`이 반영됐다.
- 5초 타이머 만료 뒤 교사 화면에는 ①번 `1명 (100%)`, 학생 화면에는 `⭕ 정답입니다`와 해설이 자동 공개됐다. 공개 전에는 정답과 분포가 보이지 않았고, 교사·학생 모두 같은 남은 시간(4초, 이후 0초)을 표시했다.
- 교사와 학생 탭의 오류 로그는 비어 있었다. 세션 코드 생성 중 일시적인 Firestore `unavailable` 재시도가 한 번 관찰됐으나 트랜잭션 재시도로 정상 생성됐다.
- 원본에 복수정답·O/X·단답형·서술형 및 이미지 문항이 없어 해당 실제 데이터 시나리오는 브라우저에서 재현하지 않았다. 이 기능과 CSV·관리자·기간 삭제·세트 삭제 거부는 59개 자동 테스트에서만 검증됐다. 따라서 이 기록은 전체 수동 체크리스트의 완료를 뜻하지 않으며, 운영 배포 뒤 원본 객관식 핵심 흐름을 다시 확인한다.
