{
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:rules": "firebase emulators:exec --only firestore --project demo-video-quiz \"node --test --test-concurrency=1 tests/firestore-rules.test.js tests/legacy-migration-admin-emulator.test.js\"",
    "migrate:legacy": "node scripts/migrate-legacy-ownership.js",
    "migrate:lifecycle": "node scripts/migrate-lifecycle-state.js",
    "migrate:counters": "node scripts/migrate-set-counters.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^10.12.0",
    "firebase-admin": "^13.4.0",
    "firebase-tools": "^14.0.0"
  }
}
